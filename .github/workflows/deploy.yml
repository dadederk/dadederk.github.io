name: Deploy to GitHub Pages

on:
  push:
    branches: [ master ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Swift
        run: |
          wget -q https://download.swift.org/swift-5.9.3-release/ubuntu2204/swift-5.9.3-RELEASE/swift-5.9.3-RELEASE-ubuntu22.04.tar.gz
          tar xzf swift-5.9.3-RELEASE-ubuntu22.04.tar.gz
          echo "$PWD/swift-5.9.3-RELEASE-ubuntu22.04/usr/bin" >> $GITHUB_PATH

      - name: Verify Swift Installation
        run: swift --version

      - name: Build site
        run: |
          cd AccessibilityUpTo11
          swift run AccessibilityUpTo11 build

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: AccessibilityUpTo11/Build

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        
